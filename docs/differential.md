# Differential against mutmut, per mutant (milestone M1.3)

Generated by `scripts/differential.py`. Do not edit by hand: a later
run rewrites it, and M1.3.5 uses the rewrite to detect drift.

Two tools, joined on what the mutation *is* — `(module, original line,
mutated line)` — rather than on any identifier, because they share none.
mutmut is never authoritative here; a disagreement is a question with
four permitted answers, and every one of them has been given.

**10 projects compared.**

The five open-source libraries from M4 are **not** among them, and
M1.3.1 names them, so the reason is here rather than left to be
noticed: mutmut requires rewriting each project's pytest configuration and running from a generated `mutants/` tree, plus a virtualenv per target carrying both mutmut and that project's dependencies. Generated projects make up the count
instead. That is a weaker substitution and worth saying so — generated
code has no decorators, no classes, no closures and no third-party
imports, so it cannot surface the disagreements those produce.

| project | moonbuggy mutants | mutmut mutants | shared | agree | disagree | ambiguous |
|---|---:|---:|---:|---:|---:|---:|
| fixture | 29 | 44 | 16 | 15 | 2 | 2 |
| gen-wide | 192 | 216 | 72 | 0 | 88 | 48 |
| gen-deep | 128 | 144 | 48 | 0 | 52 | 32 |
| gen-sparse | 160 | 180 | 60 | 0 | 70 | 40 |
| gen-dense | 32 | 36 | 12 | 0 | 16 | 8 |
| gen-slow | 48 | 54 | 18 | 18 | 4 | 12 |
| gen-tiny | 16 | 18 | 6 | 0 | 8 | 4 |
| gen-flat | 96 | 108 | 60 | 12 | 48 | 0 |
| gen-tall | 96 | 108 | 36 | 0 | 38 | 24 |
| gen-uncovered | 144 | 162 | 54 | 0 | 60 | 36 |

**Agreement on shared mutants: 45/382 (11.8%).**

## Disagreements by category (M1.3.2)

| category | count |
|---|---:|
| genuine semantic difference | 337 |
| not actually the same mutant | 49 |

## Every disagreement

- **fixture · inventory.py** — `return 0` → `return 1`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **fixture · loops.py** — `total = 0` → `total = 1`
  - moonbuggy `['KILLED', 'KILLED']`, mutmut `['KILLED', 'KILLED']`
  - **not actually the same mutant**: the change `total = 0` -> `total = 1` occurs 2 times in loops.py, so the join is ambiguous and no pairing can be asserted
- **gen-wide · mod_0.py** — `if i % 2 == 0:` → `if i % 2 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_0.py** — `if i % 2 == 0:` → `if i % 2 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_0.py** — `if i % 2 == 0:` → `if i % 3 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_0.py** — `if i % 3 == 0:` → `if i % 3 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_0.py** — `if i % 3 == 0:` → `if i % 3 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_0.py** — `if i % 3 == 0:` → `if i % 4 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_0.py** — `if i % 4 == 0:` → `if i % 4 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_0.py** — `if i % 4 == 0:` → `if i % 4 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_0.py** — `if i % 4 == 0:` → `if i % 5 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_0.py** — `total += i` → `total -= i`
  - moonbuggy `['NO_COVERAGE', 'NO_COVERAGE', 'NO_COVERAGE']`, mutmut `['SURVIVED', 'SURVIVED', 'SURVIVED']`
  - **not actually the same mutant**: the change `total += i` -> `total -= i` occurs 3 times in mod_0.py, so the join is ambiguous and no pairing can be asserted
- **gen-wide · mod_0.py** — `total = 0` → `total = 1`
  - moonbuggy `['SURVIVED', 'SURVIVED', 'SURVIVED']`, mutmut `['SURVIVED', 'SURVIVED', 'SURVIVED']`
  - **not actually the same mutant**: the change `total = 0` -> `total = 1` occurs 3 times in mod_0.py, so the join is ambiguous and no pairing can be asserted
- **gen-wide · mod_1.py** — `if i % 2 == 0:` → `if i % 2 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_1.py** — `if i % 2 == 0:` → `if i % 2 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_1.py** — `if i % 2 == 0:` → `if i % 3 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_1.py** — `if i % 3 == 0:` → `if i % 3 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_1.py** — `if i % 3 == 0:` → `if i % 3 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_1.py** — `if i % 3 == 0:` → `if i % 4 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_1.py** — `if i % 4 == 0:` → `if i % 4 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_1.py** — `if i % 4 == 0:` → `if i % 4 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_1.py** — `if i % 4 == 0:` → `if i % 5 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_1.py** — `total += i` → `total -= i`
  - moonbuggy `['NO_COVERAGE', 'NO_COVERAGE', 'NO_COVERAGE']`, mutmut `['SURVIVED', 'SURVIVED', 'SURVIVED']`
  - **not actually the same mutant**: the change `total += i` -> `total -= i` occurs 3 times in mod_1.py, so the join is ambiguous and no pairing can be asserted
- **gen-wide · mod_1.py** — `total = 0` → `total = 1`
  - moonbuggy `['SURVIVED', 'SURVIVED', 'SURVIVED']`, mutmut `['SURVIVED', 'SURVIVED', 'SURVIVED']`
  - **not actually the same mutant**: the change `total = 0` -> `total = 1` occurs 3 times in mod_1.py, so the join is ambiguous and no pairing can be asserted
- **gen-wide · mod_2.py** — `if i % 2 == 0:` → `if i % 2 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_2.py** — `if i % 2 == 0:` → `if i % 2 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_2.py** — `if i % 2 == 0:` → `if i % 3 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_2.py** — `if i % 3 == 0:` → `if i % 3 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_2.py** — `if i % 3 == 0:` → `if i % 3 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_2.py** — `if i % 3 == 0:` → `if i % 4 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_2.py** — `if i % 4 == 0:` → `if i % 4 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_2.py** — `if i % 4 == 0:` → `if i % 4 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_2.py** — `if i % 4 == 0:` → `if i % 5 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_2.py** — `total += i` → `total -= i`
  - moonbuggy `['NO_COVERAGE', 'NO_COVERAGE', 'NO_COVERAGE']`, mutmut `['SURVIVED', 'SURVIVED', 'SURVIVED']`
  - **not actually the same mutant**: the change `total += i` -> `total -= i` occurs 3 times in mod_2.py, so the join is ambiguous and no pairing can be asserted
- **gen-wide · mod_2.py** — `total = 0` → `total = 1`
  - moonbuggy `['SURVIVED', 'SURVIVED', 'SURVIVED']`, mutmut `['SURVIVED', 'SURVIVED', 'SURVIVED']`
  - **not actually the same mutant**: the change `total = 0` -> `total = 1` occurs 3 times in mod_2.py, so the join is ambiguous and no pairing can be asserted
- **gen-wide · mod_3.py** — `if i % 2 == 0:` → `if i % 2 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_3.py** — `if i % 2 == 0:` → `if i % 2 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_3.py** — `if i % 2 == 0:` → `if i % 3 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_3.py** — `if i % 3 == 0:` → `if i % 3 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_3.py** — `if i % 3 == 0:` → `if i % 3 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_3.py** — `if i % 3 == 0:` → `if i % 4 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_3.py** — `if i % 4 == 0:` → `if i % 4 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_3.py** — `if i % 4 == 0:` → `if i % 4 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_3.py** — `if i % 4 == 0:` → `if i % 5 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_3.py** — `total += i` → `total -= i`
  - moonbuggy `['NO_COVERAGE', 'NO_COVERAGE', 'NO_COVERAGE']`, mutmut `['SURVIVED', 'SURVIVED', 'SURVIVED']`
  - **not actually the same mutant**: the change `total += i` -> `total -= i` occurs 3 times in mod_3.py, so the join is ambiguous and no pairing can be asserted
- **gen-wide · mod_3.py** — `total = 0` → `total = 1`
  - moonbuggy `['SURVIVED', 'SURVIVED', 'SURVIVED']`, mutmut `['SURVIVED', 'SURVIVED', 'SURVIVED']`
  - **not actually the same mutant**: the change `total = 0` -> `total = 1` occurs 3 times in mod_3.py, so the join is ambiguous and no pairing can be asserted
- **gen-wide · mod_4.py** — `if i % 2 == 0:` → `if i % 2 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_4.py** — `if i % 2 == 0:` → `if i % 2 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_4.py** — `if i % 2 == 0:` → `if i % 3 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_4.py** — `if i % 3 == 0:` → `if i % 3 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_4.py** — `if i % 3 == 0:` → `if i % 3 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_4.py** — `if i % 3 == 0:` → `if i % 4 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_4.py** — `if i % 4 == 0:` → `if i % 4 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_4.py** — `if i % 4 == 0:` → `if i % 4 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_4.py** — `if i % 4 == 0:` → `if i % 5 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_4.py** — `total += i` → `total -= i`
  - moonbuggy `['NO_COVERAGE', 'NO_COVERAGE', 'NO_COVERAGE']`, mutmut `['SURVIVED', 'SURVIVED', 'SURVIVED']`
  - **not actually the same mutant**: the change `total += i` -> `total -= i` occurs 3 times in mod_4.py, so the join is ambiguous and no pairing can be asserted
- **gen-wide · mod_4.py** — `total = 0` → `total = 1`
  - moonbuggy `['SURVIVED', 'SURVIVED', 'SURVIVED']`, mutmut `['SURVIVED', 'SURVIVED', 'SURVIVED']`
  - **not actually the same mutant**: the change `total = 0` -> `total = 1` occurs 3 times in mod_4.py, so the join is ambiguous and no pairing can be asserted
- **gen-wide · mod_5.py** — `if i % 2 == 0:` → `if i % 2 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_5.py** — `if i % 2 == 0:` → `if i % 2 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_5.py** — `if i % 2 == 0:` → `if i % 3 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_5.py** — `if i % 3 == 0:` → `if i % 3 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_5.py** — `if i % 3 == 0:` → `if i % 3 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_5.py** — `if i % 3 == 0:` → `if i % 4 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_5.py** — `if i % 4 == 0:` → `if i % 4 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_5.py** — `if i % 4 == 0:` → `if i % 4 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_5.py** — `if i % 4 == 0:` → `if i % 5 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_5.py** — `total += i` → `total -= i`
  - moonbuggy `['NO_COVERAGE', 'NO_COVERAGE', 'NO_COVERAGE']`, mutmut `['SURVIVED', 'SURVIVED', 'SURVIVED']`
  - **not actually the same mutant**: the change `total += i` -> `total -= i` occurs 3 times in mod_5.py, so the join is ambiguous and no pairing can be asserted
- **gen-wide · mod_5.py** — `total = 0` → `total = 1`
  - moonbuggy `['SURVIVED', 'SURVIVED', 'SURVIVED']`, mutmut `['SURVIVED', 'SURVIVED', 'SURVIVED']`
  - **not actually the same mutant**: the change `total = 0` -> `total = 1` occurs 3 times in mod_5.py, so the join is ambiguous and no pairing can be asserted
- **gen-wide · mod_6.py** — `if i % 2 == 0:` → `if i % 2 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_6.py** — `if i % 2 == 0:` → `if i % 2 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_6.py** — `if i % 2 == 0:` → `if i % 3 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_6.py** — `if i % 3 == 0:` → `if i % 3 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_6.py** — `if i % 3 == 0:` → `if i % 3 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_6.py** — `if i % 3 == 0:` → `if i % 4 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_6.py** — `if i % 4 == 0:` → `if i % 4 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_6.py** — `if i % 4 == 0:` → `if i % 4 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_6.py** — `if i % 4 == 0:` → `if i % 5 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_6.py** — `total += i` → `total -= i`
  - moonbuggy `['NO_COVERAGE', 'NO_COVERAGE', 'NO_COVERAGE']`, mutmut `['SURVIVED', 'SURVIVED', 'SURVIVED']`
  - **not actually the same mutant**: the change `total += i` -> `total -= i` occurs 3 times in mod_6.py, so the join is ambiguous and no pairing can be asserted
- **gen-wide · mod_6.py** — `total = 0` → `total = 1`
  - moonbuggy `['SURVIVED', 'SURVIVED', 'SURVIVED']`, mutmut `['SURVIVED', 'SURVIVED', 'SURVIVED']`
  - **not actually the same mutant**: the change `total = 0` -> `total = 1` occurs 3 times in mod_6.py, so the join is ambiguous and no pairing can be asserted
- **gen-wide · mod_7.py** — `if i % 2 == 0:` → `if i % 2 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_7.py** — `if i % 2 == 0:` → `if i % 2 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_7.py** — `if i % 2 == 0:` → `if i % 3 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_7.py** — `if i % 3 == 0:` → `if i % 3 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_7.py** — `if i % 3 == 0:` → `if i % 3 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_7.py** — `if i % 3 == 0:` → `if i % 4 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_7.py** — `if i % 4 == 0:` → `if i % 4 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_7.py** — `if i % 4 == 0:` → `if i % 4 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_7.py** — `if i % 4 == 0:` → `if i % 5 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-wide · mod_7.py** — `total += i` → `total -= i`
  - moonbuggy `['NO_COVERAGE', 'NO_COVERAGE', 'NO_COVERAGE']`, mutmut `['SURVIVED', 'SURVIVED', 'SURVIVED']`
  - **not actually the same mutant**: the change `total += i` -> `total -= i` occurs 3 times in mod_7.py, so the join is ambiguous and no pairing can be asserted
- **gen-wide · mod_7.py** — `total = 0` → `total = 1`
  - moonbuggy `['SURVIVED', 'SURVIVED', 'SURVIVED']`, mutmut `['SURVIVED', 'SURVIVED', 'SURVIVED']`
  - **not actually the same mutant**: the change `total = 0` -> `total = 1` occurs 3 times in mod_7.py, so the join is ambiguous and no pairing can be asserted
- **gen-deep · mod_0.py** — `if i % 2 == 0:` → `if i % 2 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-deep · mod_0.py** — `if i % 2 == 0:` → `if i % 2 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-deep · mod_0.py** — `if i % 2 == 0:` → `if i % 3 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-deep · mod_0.py** — `if i % 3 == 0:` → `if i % 3 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-deep · mod_0.py** — `if i % 3 == 0:` → `if i % 3 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-deep · mod_0.py** — `if i % 3 == 0:` → `if i % 4 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-deep · mod_0.py** — `if i % 4 == 0:` → `if i % 4 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-deep · mod_0.py** — `if i % 4 == 0:` → `if i % 4 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-deep · mod_0.py** — `if i % 4 == 0:` → `if i % 5 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-deep · mod_0.py** — `if i % 5 == 0:` → `if i % 5 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-deep · mod_0.py** — `if i % 5 == 0:` → `if i % 5 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-deep · mod_0.py** — `if i % 5 == 0:` → `if i % 6 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-deep · mod_0.py** — `if i % 6 == 0:` → `if i % 6 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-deep · mod_0.py** — `if i % 6 == 0:` → `if i % 6 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-deep · mod_0.py** — `if i % 6 == 0:` → `if i % 7 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-deep · mod_0.py** — `if i % 7 == 0:` → `if i % 7 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-deep · mod_0.py** — `if i % 7 == 0:` → `if i % 7 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-deep · mod_0.py** — `if i % 7 == 0:` → `if i % 8 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-deep · mod_0.py** — `if i % 8 == 0:` → `if i % 8 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-deep · mod_0.py** — `if i % 8 == 0:` → `if i % 8 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-deep · mod_0.py** — `if i % 8 == 0:` → `if i % 9 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-deep · mod_0.py** — `if i % 9 == 0:` → `if i % 10 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-deep · mod_0.py** — `if i % 9 == 0:` → `if i % 9 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-deep · mod_0.py** — `if i % 9 == 0:` → `if i % 9 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-deep · mod_0.py** — `total += i` → `total -= i`
  - moonbuggy `['NO_COVERAGE', 'NO_COVERAGE', 'NO_COVERAGE', 'NO_COVERAGE', 'NO_COVERAGE', 'NO_COVERAGE', 'NO_COVERAGE', 'NO_COVERAGE']`, mutmut `['SURVIVED', 'SURVIVED', 'SURVIVED', 'SURVIVED', 'SURVIVED', 'SURVIVED', 'SURVIVED', 'SURVIVED']`
  - **not actually the same mutant**: the change `total += i` -> `total -= i` occurs 8 times in mod_0.py, so the join is ambiguous and no pairing can be asserted
- **gen-deep · mod_0.py** — `total = 0` → `total = 1`
  - moonbuggy `['SURVIVED', 'SURVIVED', 'SURVIVED', 'SURVIVED', 'SURVIVED', 'SURVIVED', 'SURVIVED', 'SURVIVED']`, mutmut `['SURVIVED', 'SURVIVED', 'SURVIVED', 'SURVIVED', 'SURVIVED', 'SURVIVED', 'SURVIVED', 'SURVIVED']`
  - **not actually the same mutant**: the change `total = 0` -> `total = 1` occurs 8 times in mod_0.py, so the join is ambiguous and no pairing can be asserted
- **gen-deep · mod_1.py** — `if i % 2 == 0:` → `if i % 2 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-deep · mod_1.py** — `if i % 2 == 0:` → `if i % 2 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-deep · mod_1.py** — `if i % 2 == 0:` → `if i % 3 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-deep · mod_1.py** — `if i % 3 == 0:` → `if i % 3 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-deep · mod_1.py** — `if i % 3 == 0:` → `if i % 3 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-deep · mod_1.py** — `if i % 3 == 0:` → `if i % 4 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-deep · mod_1.py** — `if i % 4 == 0:` → `if i % 4 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-deep · mod_1.py** — `if i % 4 == 0:` → `if i % 4 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-deep · mod_1.py** — `if i % 4 == 0:` → `if i % 5 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-deep · mod_1.py** — `if i % 5 == 0:` → `if i % 5 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-deep · mod_1.py** — `if i % 5 == 0:` → `if i % 5 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-deep · mod_1.py** — `if i % 5 == 0:` → `if i % 6 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-deep · mod_1.py** — `if i % 6 == 0:` → `if i % 6 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-deep · mod_1.py** — `if i % 6 == 0:` → `if i % 6 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-deep · mod_1.py** — `if i % 6 == 0:` → `if i % 7 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-deep · mod_1.py** — `if i % 7 == 0:` → `if i % 7 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-deep · mod_1.py** — `if i % 7 == 0:` → `if i % 7 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-deep · mod_1.py** — `if i % 7 == 0:` → `if i % 8 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-deep · mod_1.py** — `if i % 8 == 0:` → `if i % 8 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-deep · mod_1.py** — `if i % 8 == 0:` → `if i % 8 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-deep · mod_1.py** — `if i % 8 == 0:` → `if i % 9 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-deep · mod_1.py** — `if i % 9 == 0:` → `if i % 10 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-deep · mod_1.py** — `if i % 9 == 0:` → `if i % 9 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-deep · mod_1.py** — `if i % 9 == 0:` → `if i % 9 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-deep · mod_1.py** — `total += i` → `total -= i`
  - moonbuggy `['NO_COVERAGE', 'NO_COVERAGE', 'NO_COVERAGE', 'NO_COVERAGE', 'NO_COVERAGE', 'NO_COVERAGE', 'NO_COVERAGE', 'NO_COVERAGE']`, mutmut `['SURVIVED', 'SURVIVED', 'SURVIVED', 'SURVIVED', 'SURVIVED', 'SURVIVED', 'SURVIVED', 'SURVIVED']`
  - **not actually the same mutant**: the change `total += i` -> `total -= i` occurs 8 times in mod_1.py, so the join is ambiguous and no pairing can be asserted
- **gen-deep · mod_1.py** — `total = 0` → `total = 1`
  - moonbuggy `['SURVIVED', 'SURVIVED', 'SURVIVED', 'SURVIVED', 'SURVIVED', 'SURVIVED', 'SURVIVED', 'SURVIVED']`, mutmut `['SURVIVED', 'SURVIVED', 'SURVIVED', 'SURVIVED', 'SURVIVED', 'SURVIVED', 'SURVIVED', 'SURVIVED']`
  - **not actually the same mutant**: the change `total = 0` -> `total = 1` occurs 8 times in mod_1.py, so the join is ambiguous and no pairing can be asserted
- **gen-sparse · mod_0.py** — `if i % 2 == 0:` → `if i % 2 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-sparse · mod_0.py** — `if i % 2 == 0:` → `if i % 2 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-sparse · mod_0.py** — `if i % 2 == 0:` → `if i % 3 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-sparse · mod_0.py** — `if i % 3 == 0:` → `if i % 3 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-sparse · mod_0.py** — `if i % 3 == 0:` → `if i % 3 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-sparse · mod_0.py** — `if i % 3 == 0:` → `if i % 4 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-sparse · mod_0.py** — `if i % 4 == 0:` → `if i % 4 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-sparse · mod_0.py** — `if i % 4 == 0:` → `if i % 4 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-sparse · mod_0.py** — `if i % 4 == 0:` → `if i % 5 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-sparse · mod_0.py** — `if i % 5 == 0:` → `if i % 5 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-sparse · mod_0.py** — `if i % 5 == 0:` → `if i % 5 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-sparse · mod_0.py** — `if i % 5 == 0:` → `if i % 6 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-sparse · mod_0.py** — `total += i` → `total -= i`
  - moonbuggy `['NO_COVERAGE', 'NO_COVERAGE', 'NO_COVERAGE', 'NO_COVERAGE']`, mutmut `['SURVIVED', 'SURVIVED', 'SUSPICIOUS', 'SUSPICIOUS']`
  - **not actually the same mutant**: the change `total += i` -> `total -= i` occurs 4 times in mod_0.py, so the join is ambiguous and no pairing can be asserted
- **gen-sparse · mod_0.py** — `total = 0` → `total = 1`
  - moonbuggy `['SURVIVED', 'SURVIVED', 'NO_COVERAGE', 'NO_COVERAGE']`, mutmut `['SURVIVED', 'SURVIVED', 'SUSPICIOUS', 'SUSPICIOUS']`
  - **not actually the same mutant**: the change `total = 0` -> `total = 1` occurs 4 times in mod_0.py, so the join is ambiguous and no pairing can be asserted
- **gen-sparse · mod_1.py** — `if i % 2 == 0:` → `if i % 2 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-sparse · mod_1.py** — `if i % 2 == 0:` → `if i % 2 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-sparse · mod_1.py** — `if i % 2 == 0:` → `if i % 3 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-sparse · mod_1.py** — `if i % 3 == 0:` → `if i % 3 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-sparse · mod_1.py** — `if i % 3 == 0:` → `if i % 3 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-sparse · mod_1.py** — `if i % 3 == 0:` → `if i % 4 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-sparse · mod_1.py** — `if i % 4 == 0:` → `if i % 4 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-sparse · mod_1.py** — `if i % 4 == 0:` → `if i % 4 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-sparse · mod_1.py** — `if i % 4 == 0:` → `if i % 5 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-sparse · mod_1.py** — `if i % 5 == 0:` → `if i % 5 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-sparse · mod_1.py** — `if i % 5 == 0:` → `if i % 5 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-sparse · mod_1.py** — `if i % 5 == 0:` → `if i % 6 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-sparse · mod_1.py** — `total += i` → `total -= i`
  - moonbuggy `['NO_COVERAGE', 'NO_COVERAGE', 'NO_COVERAGE', 'NO_COVERAGE']`, mutmut `['SURVIVED', 'SURVIVED', 'SUSPICIOUS', 'SUSPICIOUS']`
  - **not actually the same mutant**: the change `total += i` -> `total -= i` occurs 4 times in mod_1.py, so the join is ambiguous and no pairing can be asserted
- **gen-sparse · mod_1.py** — `total = 0` → `total = 1`
  - moonbuggy `['SURVIVED', 'SURVIVED', 'NO_COVERAGE', 'NO_COVERAGE']`, mutmut `['SURVIVED', 'SURVIVED', 'SUSPICIOUS', 'SUSPICIOUS']`
  - **not actually the same mutant**: the change `total = 0` -> `total = 1` occurs 4 times in mod_1.py, so the join is ambiguous and no pairing can be asserted
- **gen-sparse · mod_2.py** — `if i % 2 == 0:` → `if i % 2 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-sparse · mod_2.py** — `if i % 2 == 0:` → `if i % 2 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-sparse · mod_2.py** — `if i % 2 == 0:` → `if i % 3 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-sparse · mod_2.py** — `if i % 3 == 0:` → `if i % 3 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-sparse · mod_2.py** — `if i % 3 == 0:` → `if i % 3 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-sparse · mod_2.py** — `if i % 3 == 0:` → `if i % 4 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-sparse · mod_2.py** — `if i % 4 == 0:` → `if i % 4 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-sparse · mod_2.py** — `if i % 4 == 0:` → `if i % 4 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-sparse · mod_2.py** — `if i % 4 == 0:` → `if i % 5 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-sparse · mod_2.py** — `if i % 5 == 0:` → `if i % 5 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-sparse · mod_2.py** — `if i % 5 == 0:` → `if i % 5 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-sparse · mod_2.py** — `if i % 5 == 0:` → `if i % 6 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-sparse · mod_2.py** — `total += i` → `total -= i`
  - moonbuggy `['NO_COVERAGE', 'NO_COVERAGE', 'NO_COVERAGE', 'NO_COVERAGE']`, mutmut `['SURVIVED', 'SURVIVED', 'SUSPICIOUS', 'SUSPICIOUS']`
  - **not actually the same mutant**: the change `total += i` -> `total -= i` occurs 4 times in mod_2.py, so the join is ambiguous and no pairing can be asserted
- **gen-sparse · mod_2.py** — `total = 0` → `total = 1`
  - moonbuggy `['SURVIVED', 'SURVIVED', 'NO_COVERAGE', 'NO_COVERAGE']`, mutmut `['SURVIVED', 'SURVIVED', 'SUSPICIOUS', 'SUSPICIOUS']`
  - **not actually the same mutant**: the change `total = 0` -> `total = 1` occurs 4 times in mod_2.py, so the join is ambiguous and no pairing can be asserted
- **gen-sparse · mod_3.py** — `if i % 2 == 0:` → `if i % 2 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-sparse · mod_3.py** — `if i % 2 == 0:` → `if i % 2 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-sparse · mod_3.py** — `if i % 2 == 0:` → `if i % 3 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-sparse · mod_3.py** — `if i % 3 == 0:` → `if i % 3 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-sparse · mod_3.py** — `if i % 3 == 0:` → `if i % 3 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-sparse · mod_3.py** — `if i % 3 == 0:` → `if i % 4 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-sparse · mod_3.py** — `if i % 4 == 0:` → `if i % 4 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-sparse · mod_3.py** — `if i % 4 == 0:` → `if i % 4 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-sparse · mod_3.py** — `if i % 4 == 0:` → `if i % 5 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-sparse · mod_3.py** — `if i % 5 == 0:` → `if i % 5 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-sparse · mod_3.py** — `if i % 5 == 0:` → `if i % 5 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-sparse · mod_3.py** — `if i % 5 == 0:` → `if i % 6 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-sparse · mod_3.py** — `total += i` → `total -= i`
  - moonbuggy `['NO_COVERAGE', 'NO_COVERAGE', 'NO_COVERAGE', 'NO_COVERAGE']`, mutmut `['SURVIVED', 'SURVIVED', 'SUSPICIOUS', 'SUSPICIOUS']`
  - **not actually the same mutant**: the change `total += i` -> `total -= i` occurs 4 times in mod_3.py, so the join is ambiguous and no pairing can be asserted
- **gen-sparse · mod_3.py** — `total = 0` → `total = 1`
  - moonbuggy `['SURVIVED', 'SURVIVED', 'NO_COVERAGE', 'NO_COVERAGE']`, mutmut `['SURVIVED', 'SURVIVED', 'SUSPICIOUS', 'SUSPICIOUS']`
  - **not actually the same mutant**: the change `total = 0` -> `total = 1` occurs 4 times in mod_3.py, so the join is ambiguous and no pairing can be asserted
- **gen-sparse · mod_4.py** — `if i % 2 == 0:` → `if i % 2 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-sparse · mod_4.py** — `if i % 2 == 0:` → `if i % 2 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-sparse · mod_4.py** — `if i % 2 == 0:` → `if i % 3 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-sparse · mod_4.py** — `if i % 3 == 0:` → `if i % 3 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-sparse · mod_4.py** — `if i % 3 == 0:` → `if i % 3 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-sparse · mod_4.py** — `if i % 3 == 0:` → `if i % 4 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-sparse · mod_4.py** — `if i % 4 == 0:` → `if i % 4 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-sparse · mod_4.py** — `if i % 4 == 0:` → `if i % 4 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-sparse · mod_4.py** — `if i % 4 == 0:` → `if i % 5 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-sparse · mod_4.py** — `if i % 5 == 0:` → `if i % 5 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-sparse · mod_4.py** — `if i % 5 == 0:` → `if i % 5 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-sparse · mod_4.py** — `if i % 5 == 0:` → `if i % 6 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-sparse · mod_4.py** — `total += i` → `total -= i`
  - moonbuggy `['NO_COVERAGE', 'NO_COVERAGE', 'NO_COVERAGE', 'NO_COVERAGE']`, mutmut `['SURVIVED', 'SURVIVED', 'SUSPICIOUS', 'SUSPICIOUS']`
  - **not actually the same mutant**: the change `total += i` -> `total -= i` occurs 4 times in mod_4.py, so the join is ambiguous and no pairing can be asserted
- **gen-sparse · mod_4.py** — `total = 0` → `total = 1`
  - moonbuggy `['SURVIVED', 'SURVIVED', 'NO_COVERAGE', 'NO_COVERAGE']`, mutmut `['SURVIVED', 'SURVIVED', 'SUSPICIOUS', 'SUSPICIOUS']`
  - **not actually the same mutant**: the change `total = 0` -> `total = 1` occurs 4 times in mod_4.py, so the join is ambiguous and no pairing can be asserted
- **gen-dense · mod_0.py** — `if i % 2 == 0:` → `if i % 2 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-dense · mod_0.py** — `if i % 2 == 0:` → `if i % 2 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-dense · mod_0.py** — `if i % 2 == 0:` → `if i % 3 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-dense · mod_0.py** — `if i % 3 == 0:` → `if i % 3 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-dense · mod_0.py** — `if i % 3 == 0:` → `if i % 3 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-dense · mod_0.py** — `if i % 3 == 0:` → `if i % 4 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-dense · mod_0.py** — `total += i` → `total -= i`
  - moonbuggy `['NO_COVERAGE', 'NO_COVERAGE']`, mutmut `['SURVIVED', 'SURVIVED']`
  - **not actually the same mutant**: the change `total += i` -> `total -= i` occurs 2 times in mod_0.py, so the join is ambiguous and no pairing can be asserted
- **gen-dense · mod_0.py** — `total = 0` → `total = 1`
  - moonbuggy `['SURVIVED', 'SURVIVED']`, mutmut `['SURVIVED', 'SURVIVED']`
  - **not actually the same mutant**: the change `total = 0` -> `total = 1` occurs 2 times in mod_0.py, so the join is ambiguous and no pairing can be asserted
- **gen-dense · mod_1.py** — `if i % 2 == 0:` → `if i % 2 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-dense · mod_1.py** — `if i % 2 == 0:` → `if i % 2 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-dense · mod_1.py** — `if i % 2 == 0:` → `if i % 3 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-dense · mod_1.py** — `if i % 3 == 0:` → `if i % 3 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-dense · mod_1.py** — `if i % 3 == 0:` → `if i % 3 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-dense · mod_1.py** — `if i % 3 == 0:` → `if i % 4 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-dense · mod_1.py** — `total += i` → `total -= i`
  - moonbuggy `['NO_COVERAGE', 'NO_COVERAGE']`, mutmut `['SURVIVED', 'SURVIVED']`
  - **not actually the same mutant**: the change `total += i` -> `total -= i` occurs 2 times in mod_1.py, so the join is ambiguous and no pairing can be asserted
- **gen-dense · mod_1.py** — `total = 0` → `total = 1`
  - moonbuggy `['SURVIVED', 'SURVIVED']`, mutmut `['SURVIVED', 'SURVIVED']`
  - **not actually the same mutant**: the change `total = 0` -> `total = 1` occurs 2 times in mod_1.py, so the join is ambiguous and no pairing can be asserted
- **gen-slow · mod_0.py** — `total += i` → `total -= i`
  - moonbuggy `['KILLED', 'KILLED', 'KILLED']`, mutmut `['KILLED', 'KILLED', 'KILLED']`
  - **not actually the same mutant**: the change `total += i` -> `total -= i` occurs 3 times in mod_0.py, so the join is ambiguous and no pairing can be asserted
- **gen-slow · mod_0.py** — `total = 0` → `total = 1`
  - moonbuggy `['SURVIVED', 'SURVIVED', 'SURVIVED']`, mutmut `['SURVIVED', 'SURVIVED', 'SURVIVED']`
  - **not actually the same mutant**: the change `total = 0` -> `total = 1` occurs 3 times in mod_0.py, so the join is ambiguous and no pairing can be asserted
- **gen-slow · mod_1.py** — `total += i` → `total -= i`
  - moonbuggy `['KILLED', 'KILLED', 'KILLED']`, mutmut `['KILLED', 'KILLED', 'KILLED']`
  - **not actually the same mutant**: the change `total += i` -> `total -= i` occurs 3 times in mod_1.py, so the join is ambiguous and no pairing can be asserted
- **gen-slow · mod_1.py** — `total = 0` → `total = 1`
  - moonbuggy `['SURVIVED', 'SURVIVED', 'SURVIVED']`, mutmut `['SURVIVED', 'SURVIVED', 'SURVIVED']`
  - **not actually the same mutant**: the change `total = 0` -> `total = 1` occurs 3 times in mod_1.py, so the join is ambiguous and no pairing can be asserted
- **gen-tiny · mod_0.py** — `if i % 2 == 0:` → `if i % 2 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-tiny · mod_0.py** — `if i % 2 == 0:` → `if i % 2 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-tiny · mod_0.py** — `if i % 2 == 0:` → `if i % 3 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-tiny · mod_0.py** — `if i % 3 == 0:` → `if i % 3 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-tiny · mod_0.py** — `if i % 3 == 0:` → `if i % 3 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-tiny · mod_0.py** — `if i % 3 == 0:` → `if i % 4 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-tiny · mod_0.py** — `total += i` → `total -= i`
  - moonbuggy `['NO_COVERAGE', 'NO_COVERAGE']`, mutmut `['SURVIVED', 'SURVIVED']`
  - **not actually the same mutant**: the change `total += i` -> `total -= i` occurs 2 times in mod_0.py, so the join is ambiguous and no pairing can be asserted
- **gen-tiny · mod_0.py** — `total = 0` → `total = 1`
  - moonbuggy `['SURVIVED', 'SURVIVED']`, mutmut `['SURVIVED', 'SURVIVED']`
  - **not actually the same mutant**: the change `total = 0` -> `total = 1` occurs 2 times in mod_0.py, so the join is ambiguous and no pairing can be asserted
- **gen-flat · mod_0.py** — `if i % 2 == 0:` → `if i % 2 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-flat · mod_0.py** — `if i % 2 == 0:` → `if i % 2 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-flat · mod_0.py** — `if i % 2 == 0:` → `if i % 3 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-flat · mod_0.py** — `total += i` → `total -= i`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-flat · mod_1.py** — `if i % 2 == 0:` → `if i % 2 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-flat · mod_1.py** — `if i % 2 == 0:` → `if i % 2 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-flat · mod_1.py** — `if i % 2 == 0:` → `if i % 3 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-flat · mod_1.py** — `total += i` → `total -= i`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-flat · mod_10.py** — `if i % 2 == 0:` → `if i % 2 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-flat · mod_10.py** — `if i % 2 == 0:` → `if i % 2 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-flat · mod_10.py** — `if i % 2 == 0:` → `if i % 3 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-flat · mod_10.py** — `total += i` → `total -= i`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-flat · mod_11.py** — `if i % 2 == 0:` → `if i % 2 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-flat · mod_11.py** — `if i % 2 == 0:` → `if i % 2 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-flat · mod_11.py** — `if i % 2 == 0:` → `if i % 3 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-flat · mod_11.py** — `total += i` → `total -= i`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-flat · mod_2.py** — `if i % 2 == 0:` → `if i % 2 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-flat · mod_2.py** — `if i % 2 == 0:` → `if i % 2 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-flat · mod_2.py** — `if i % 2 == 0:` → `if i % 3 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-flat · mod_2.py** — `total += i` → `total -= i`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-flat · mod_3.py** — `if i % 2 == 0:` → `if i % 2 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-flat · mod_3.py** — `if i % 2 == 0:` → `if i % 2 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-flat · mod_3.py** — `if i % 2 == 0:` → `if i % 3 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-flat · mod_3.py** — `total += i` → `total -= i`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-flat · mod_4.py** — `if i % 2 == 0:` → `if i % 2 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-flat · mod_4.py** — `if i % 2 == 0:` → `if i % 2 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-flat · mod_4.py** — `if i % 2 == 0:` → `if i % 3 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-flat · mod_4.py** — `total += i` → `total -= i`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-flat · mod_5.py** — `if i % 2 == 0:` → `if i % 2 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-flat · mod_5.py** — `if i % 2 == 0:` → `if i % 2 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-flat · mod_5.py** — `if i % 2 == 0:` → `if i % 3 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-flat · mod_5.py** — `total += i` → `total -= i`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-flat · mod_6.py** — `if i % 2 == 0:` → `if i % 2 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-flat · mod_6.py** — `if i % 2 == 0:` → `if i % 2 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-flat · mod_6.py** — `if i % 2 == 0:` → `if i % 3 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-flat · mod_6.py** — `total += i` → `total -= i`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-flat · mod_7.py** — `if i % 2 == 0:` → `if i % 2 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-flat · mod_7.py** — `if i % 2 == 0:` → `if i % 2 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-flat · mod_7.py** — `if i % 2 == 0:` → `if i % 3 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-flat · mod_7.py** — `total += i` → `total -= i`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-flat · mod_8.py** — `if i % 2 == 0:` → `if i % 2 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-flat · mod_8.py** — `if i % 2 == 0:` → `if i % 2 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-flat · mod_8.py** — `if i % 2 == 0:` → `if i % 3 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-flat · mod_8.py** — `total += i` → `total -= i`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-flat · mod_9.py** — `if i % 2 == 0:` → `if i % 2 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-flat · mod_9.py** — `if i % 2 == 0:` → `if i % 2 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-flat · mod_9.py** — `if i % 2 == 0:` → `if i % 3 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-flat · mod_9.py** — `total += i` → `total -= i`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-tall · mod_0.py** — `if i % 10 == 0:` → `if i % 10 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-tall · mod_0.py** — `if i % 10 == 0:` → `if i % 10 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-tall · mod_0.py** — `if i % 10 == 0:` → `if i % 11 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-tall · mod_0.py** — `if i % 11 == 0:` → `if i % 11 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-tall · mod_0.py** — `if i % 11 == 0:` → `if i % 11 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-tall · mod_0.py** — `if i % 11 == 0:` → `if i % 12 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-tall · mod_0.py** — `if i % 12 == 0:` → `if i % 12 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-tall · mod_0.py** — `if i % 12 == 0:` → `if i % 12 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-tall · mod_0.py** — `if i % 12 == 0:` → `if i % 13 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-tall · mod_0.py** — `if i % 13 == 0:` → `if i % 13 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-tall · mod_0.py** — `if i % 13 == 0:` → `if i % 13 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-tall · mod_0.py** — `if i % 13 == 0:` → `if i % 14 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-tall · mod_0.py** — `if i % 2 == 0:` → `if i % 2 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-tall · mod_0.py** — `if i % 2 == 0:` → `if i % 2 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-tall · mod_0.py** — `if i % 2 == 0:` → `if i % 3 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-tall · mod_0.py** — `if i % 3 == 0:` → `if i % 3 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-tall · mod_0.py** — `if i % 3 == 0:` → `if i % 3 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-tall · mod_0.py** — `if i % 3 == 0:` → `if i % 4 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-tall · mod_0.py** — `if i % 4 == 0:` → `if i % 4 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-tall · mod_0.py** — `if i % 4 == 0:` → `if i % 4 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-tall · mod_0.py** — `if i % 4 == 0:` → `if i % 5 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-tall · mod_0.py** — `if i % 5 == 0:` → `if i % 5 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-tall · mod_0.py** — `if i % 5 == 0:` → `if i % 5 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-tall · mod_0.py** — `if i % 5 == 0:` → `if i % 6 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-tall · mod_0.py** — `if i % 6 == 0:` → `if i % 6 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-tall · mod_0.py** — `if i % 6 == 0:` → `if i % 6 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-tall · mod_0.py** — `if i % 6 == 0:` → `if i % 7 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-tall · mod_0.py** — `if i % 7 == 0:` → `if i % 7 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-tall · mod_0.py** — `if i % 7 == 0:` → `if i % 7 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-tall · mod_0.py** — `if i % 7 == 0:` → `if i % 8 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-tall · mod_0.py** — `if i % 8 == 0:` → `if i % 8 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-tall · mod_0.py** — `if i % 8 == 0:` → `if i % 8 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-tall · mod_0.py** — `if i % 8 == 0:` → `if i % 9 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-tall · mod_0.py** — `if i % 9 == 0:` → `if i % 10 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-tall · mod_0.py** — `if i % 9 == 0:` → `if i % 9 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-tall · mod_0.py** — `if i % 9 == 0:` → `if i % 9 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-tall · mod_0.py** — `total += i` → `total -= i`
  - moonbuggy `['NO_COVERAGE', 'NO_COVERAGE', 'NO_COVERAGE', 'NO_COVERAGE', 'NO_COVERAGE', 'NO_COVERAGE', 'NO_COVERAGE', 'NO_COVERAGE', 'NO_COVERAGE', 'NO_COVERAGE', 'NO_COVERAGE', 'NO_COVERAGE']`, mutmut `['SURVIVED', 'SURVIVED', 'SURVIVED', 'SURVIVED', 'SURVIVED', 'SURVIVED', 'SURVIVED', 'SURVIVED', 'SURVIVED', 'SURVIVED', 'SURVIVED', 'SURVIVED']`
  - **not actually the same mutant**: the change `total += i` -> `total -= i` occurs 12 times in mod_0.py, so the join is ambiguous and no pairing can be asserted
- **gen-tall · mod_0.py** — `total = 0` → `total = 1`
  - moonbuggy `['SURVIVED', 'SURVIVED', 'SURVIVED', 'SURVIVED', 'SURVIVED', 'SURVIVED', 'SURVIVED', 'SURVIVED', 'SURVIVED', 'SURVIVED', 'SURVIVED', 'SURVIVED']`, mutmut `['SURVIVED', 'SURVIVED', 'SURVIVED', 'SURVIVED', 'SURVIVED', 'SURVIVED', 'SURVIVED', 'SURVIVED', 'SURVIVED', 'SURVIVED', 'SURVIVED', 'SURVIVED']`
  - **not actually the same mutant**: the change `total = 0` -> `total = 1` occurs 12 times in mod_0.py, so the join is ambiguous and no pairing can be asserted
- **gen-uncovered · mod_0.py** — `if i % 2 == 0:` → `if i % 2 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-uncovered · mod_0.py** — `if i % 2 == 0:` → `if i % 2 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-uncovered · mod_0.py** — `if i % 2 == 0:` → `if i % 3 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-uncovered · mod_0.py** — `if i % 3 == 0:` → `if i % 3 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-uncovered · mod_0.py** — `if i % 3 == 0:` → `if i % 3 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-uncovered · mod_0.py** — `if i % 3 == 0:` → `if i % 4 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-uncovered · mod_0.py** — `if i % 4 == 0:` → `if i % 4 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-uncovered · mod_0.py** — `if i % 4 == 0:` → `if i % 4 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-uncovered · mod_0.py** — `if i % 4 == 0:` → `if i % 5 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-uncovered · mod_0.py** — `if i % 5 == 0:` → `if i % 5 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-uncovered · mod_0.py** — `if i % 5 == 0:` → `if i % 5 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-uncovered · mod_0.py** — `if i % 5 == 0:` → `if i % 6 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-uncovered · mod_0.py** — `if i % 6 == 0:` → `if i % 6 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-uncovered · mod_0.py** — `if i % 6 == 0:` → `if i % 6 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-uncovered · mod_0.py** — `if i % 6 == 0:` → `if i % 7 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-uncovered · mod_0.py** — `if i % 7 == 0:` → `if i % 7 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-uncovered · mod_0.py** — `if i % 7 == 0:` → `if i % 7 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-uncovered · mod_0.py** — `if i % 7 == 0:` → `if i % 8 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-uncovered · mod_0.py** — `total += i` → `total -= i`
  - moonbuggy `['NO_COVERAGE', 'NO_COVERAGE', 'NO_COVERAGE', 'NO_COVERAGE', 'NO_COVERAGE', 'NO_COVERAGE']`, mutmut `['SURVIVED', 'SUSPICIOUS', 'SUSPICIOUS', 'SUSPICIOUS', 'SUSPICIOUS', 'SUSPICIOUS']`
  - **not actually the same mutant**: the change `total += i` -> `total -= i` occurs 6 times in mod_0.py, so the join is ambiguous and no pairing can be asserted
- **gen-uncovered · mod_0.py** — `total = 0` → `total = 1`
  - moonbuggy `['SURVIVED', 'NO_COVERAGE', 'NO_COVERAGE', 'NO_COVERAGE', 'NO_COVERAGE', 'NO_COVERAGE']`, mutmut `['SURVIVED', 'SUSPICIOUS', 'SUSPICIOUS', 'SUSPICIOUS', 'SUSPICIOUS', 'SUSPICIOUS']`
  - **not actually the same mutant**: the change `total = 0` -> `total = 1` occurs 6 times in mod_0.py, so the join is ambiguous and no pairing can be asserted
- **gen-uncovered · mod_1.py** — `if i % 2 == 0:` → `if i % 2 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-uncovered · mod_1.py** — `if i % 2 == 0:` → `if i % 2 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-uncovered · mod_1.py** — `if i % 2 == 0:` → `if i % 3 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-uncovered · mod_1.py** — `if i % 3 == 0:` → `if i % 3 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-uncovered · mod_1.py** — `if i % 3 == 0:` → `if i % 3 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-uncovered · mod_1.py** — `if i % 3 == 0:` → `if i % 4 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-uncovered · mod_1.py** — `if i % 4 == 0:` → `if i % 4 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-uncovered · mod_1.py** — `if i % 4 == 0:` → `if i % 4 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-uncovered · mod_1.py** — `if i % 4 == 0:` → `if i % 5 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-uncovered · mod_1.py** — `if i % 5 == 0:` → `if i % 5 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-uncovered · mod_1.py** — `if i % 5 == 0:` → `if i % 5 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-uncovered · mod_1.py** — `if i % 5 == 0:` → `if i % 6 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-uncovered · mod_1.py** — `if i % 6 == 0:` → `if i % 6 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-uncovered · mod_1.py** — `if i % 6 == 0:` → `if i % 6 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-uncovered · mod_1.py** — `if i % 6 == 0:` → `if i % 7 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-uncovered · mod_1.py** — `if i % 7 == 0:` → `if i % 7 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-uncovered · mod_1.py** — `if i % 7 == 0:` → `if i % 7 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-uncovered · mod_1.py** — `if i % 7 == 0:` → `if i % 8 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-uncovered · mod_1.py** — `total += i` → `total -= i`
  - moonbuggy `['NO_COVERAGE', 'NO_COVERAGE', 'NO_COVERAGE', 'NO_COVERAGE', 'NO_COVERAGE', 'NO_COVERAGE']`, mutmut `['SURVIVED', 'SUSPICIOUS', 'SUSPICIOUS', 'SUSPICIOUS', 'SUSPICIOUS', 'SUSPICIOUS']`
  - **not actually the same mutant**: the change `total += i` -> `total -= i` occurs 6 times in mod_1.py, so the join is ambiguous and no pairing can be asserted
- **gen-uncovered · mod_1.py** — `total = 0` → `total = 1`
  - moonbuggy `['SURVIVED', 'NO_COVERAGE', 'NO_COVERAGE', 'NO_COVERAGE', 'NO_COVERAGE', 'NO_COVERAGE']`, mutmut `['SURVIVED', 'SUSPICIOUS', 'SUSPICIOUS', 'SUSPICIOUS', 'SUSPICIOUS', 'SUSPICIOUS']`
  - **not actually the same mutant**: the change `total = 0` -> `total = 1` occurs 6 times in mod_1.py, so the join is ambiguous and no pairing can be asserted
- **gen-uncovered · mod_2.py** — `if i % 2 == 0:` → `if i % 2 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-uncovered · mod_2.py** — `if i % 2 == 0:` → `if i % 2 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-uncovered · mod_2.py** — `if i % 2 == 0:` → `if i % 3 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SURVIVED`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-uncovered · mod_2.py** — `if i % 3 == 0:` → `if i % 3 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-uncovered · mod_2.py** — `if i % 3 == 0:` → `if i % 3 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-uncovered · mod_2.py** — `if i % 3 == 0:` → `if i % 4 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-uncovered · mod_2.py** — `if i % 4 == 0:` → `if i % 4 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-uncovered · mod_2.py** — `if i % 4 == 0:` → `if i % 4 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-uncovered · mod_2.py** — `if i % 4 == 0:` → `if i % 5 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-uncovered · mod_2.py** — `if i % 5 == 0:` → `if i % 5 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-uncovered · mod_2.py** — `if i % 5 == 0:` → `if i % 5 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-uncovered · mod_2.py** — `if i % 5 == 0:` → `if i % 6 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-uncovered · mod_2.py** — `if i % 6 == 0:` → `if i % 6 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-uncovered · mod_2.py** — `if i % 6 == 0:` → `if i % 6 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-uncovered · mod_2.py** — `if i % 6 == 0:` → `if i % 7 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-uncovered · mod_2.py** — `if i % 7 == 0:` → `if i % 7 != 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-uncovered · mod_2.py** — `if i % 7 == 0:` → `if i % 7 == 1:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-uncovered · mod_2.py** — `if i % 7 == 0:` → `if i % 8 == 0:`
  - moonbuggy `NO_COVERAGE`, mutmut `SUSPICIOUS`
  - **genuine semantic difference**: no test covers this line at all. moonbuggy reports NO_COVERAGE because an untested line is a finding -- a different finding from `tested but not checked`, but a finding. mutmut has no tests to run for the function and reports the run it could not make rather than the gap it found. Both are defensible; they are answers to different questions
- **gen-uncovered · mod_2.py** — `total += i` → `total -= i`
  - moonbuggy `['NO_COVERAGE', 'NO_COVERAGE', 'NO_COVERAGE', 'NO_COVERAGE', 'NO_COVERAGE', 'NO_COVERAGE']`, mutmut `['SURVIVED', 'SUSPICIOUS', 'SUSPICIOUS', 'SUSPICIOUS', 'SUSPICIOUS', 'SUSPICIOUS']`
  - **not actually the same mutant**: the change `total += i` -> `total -= i` occurs 6 times in mod_2.py, so the join is ambiguous and no pairing can be asserted
- **gen-uncovered · mod_2.py** — `total = 0` → `total = 1`
  - moonbuggy `['SURVIVED', 'NO_COVERAGE', 'NO_COVERAGE', 'NO_COVERAGE', 'NO_COVERAGE', 'NO_COVERAGE']`, mutmut `['SURVIVED', 'SUSPICIOUS', 'SUSPICIOUS', 'SUSPICIOUS', 'SUSPICIOUS', 'SUSPICIOUS']`
  - **not actually the same mutant**: the change `total = 0` -> `total = 1` occurs 6 times in mod_2.py, so the join is ambiguous and no pairing can be asserted
